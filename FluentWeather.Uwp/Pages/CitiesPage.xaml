<Page
    x:Class="FluentWeather.Uwp.Pages.CitiesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentWeather.Uwp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:FluentWeather.Abstraction.Models"
    xmlns:controls="using:FluentWeather.Uwp.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <AutoSuggestBox PlaceholderText="添加城市..." QueryIcon="Find" Margin="8,4,8,8" IsEnabled="False"/>

        <ListView Grid.Row="1" SelectedIndex="0">
            <ListView.Items>
                <controls:CityItem Location="{x:Bind ViewModel.CurrentCity}"/>
            </ListView.Items>
        </ListView>
        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Cities}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:GeolocationBase">
                    <controls:CityItem Location="{x:Bind}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
            
    </Grid>
</Page>
